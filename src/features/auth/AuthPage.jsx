import {useState} from 'react'; import {supabase} from '../../shared/supabase';
export function AuthPage(){const [mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setMsg('');const fn=mode==='login'?supabase.auth.signInWithPassword({email,password}):supabase.auth.signUp({email,password});const {error}=await fn;setMsg(error?error.message:mode==='login'?'Login realizado.':'Cadastro criado. Confirme seu e-mail, se solicitado.');setBusy(false)} async function reset(){if(!email)return setMsg('Informe seu e-mail.');const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+import.meta.env.BASE_URL});setMsg(error?.message||'Confira sua caixa de entrada.')}return <main className="auth"><section className="auth-brand"><div className="logo big">F</div><h1>Flux</h1><p>Seu trabalho em movimento.</p></section><form className="panel auth-form" onSubmit={submit}><h2>{mode==='login'?'Entrar':'Criar conta'}</h2><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength="6" required autoComplete={mode==='login'?'current-password':'new-password'}/></label>{msg&&<p className="message">{msg}</p>}<button className="primary" disabled={busy}>{busy?'Aguarde...':mode==='login'?'Entrar':'Cadastrar'}</button><button type="button" className="link" onClick={()=>setMode(m=>m==='login'?'signup':'login')}>{mode==='login'?'Não tem conta? Cadastre-se':'Já tem conta? Entre'}</button>{mode==='login'&&<button type="button" className="link" onClick={reset}>Esqueci minha senha</button>}</form></main>}
