import {useEffect,useState} from 'react'; import {PRIORITIES,STATUSES} from '../../shared/constants';
const blank={title:'',description:'',status:'todo',priority:'medium',project_id:'',start_at:'',due_at:'',position:1000};
export function TaskModal({task,projects,onClose,onSave,onDelete}){const [f,setF]=useState(blank);useEffect(()=>setF(task?{...blank,...task,start_at:task.start_at?.slice(0,16)||'',due_at:task.due_at?.slice(0,16)||''}:blank),[task]);const change=e=>setF({...f,[e.target.name]:e.target.value});return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><form className="modal panel" onSubmit={e=>{e.preventDefault();onSave(f)}}><div className="row between"><h2>{task?'Editar tarefa':'Nova tarefa'}</h2><button type="button" className="icon" onClick={onClose}>×</button></div><label>Título<input name="title" value={f.title} onChange={change} required autoFocus/></label><label>Descrição<textarea name="description" value={f.description||''} onChange={change} rows="3"/></label><div className="grid2"><label>Status<select name="status" value={f.status} onChange={change}>{STATUSES.map(([v,l])=><option key={v} value={v}>{l}</option>)}<option value="canceled">Cancelado</option></select></label><label>Prioridade<select name="priority" value={f.priority} onChange={change}>{PRIORITIES.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Projeto<select name="project_id" value={f.project_id||''} onChange={change}><option value="">Caixa de entrada</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Início<input type="datetime-local" name="start_at" value={f.start_at||''} onChange={change}/></label><label>Prazo<input type="datetime-local" name="due_at" value={f.due_at||''} onChange={change}/></label></div><div className="row between"><div>{task&&<button type="button" className="danger" onClick={()=>onDelete(task.id)}>Arquivar</button>}</div><div className="row"><button type="button" onClick={onClose}>Cancelar</button><button className="primary">Salvar</button></div></div></form></div>}
