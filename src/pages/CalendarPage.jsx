import FullCalendar from '@fullcalendar/react'; import dayGridPlugin from '@fullcalendar/daygrid'; import timeGridPlugin from '@fullcalendar/timegrid'; import listPlugin from '@fullcalendar/list'; import interactionPlugin from '@fullcalendar/interaction'; import ptBr from '@fullcalendar/core/locales/pt-br'; import {useTasks} from '../shared/useData';
export function CalendarPage(){const {data:tasks=[]}=useTasks();const events=tasks.filter(t=>t.due_at).map(t=>({id:t.id,title:t.title,start:t.start_at||t.due_at,end:t.due_at,color:t.status==='done'?'#7db61c':t.priority==='urgent'?'#e05252':'#00ae9d'}));return <main><header className="page-header"><div><p className="eyebrow">Prazos e agenda</p><h1>Calendário</h1></div></header><section className="panel calendar"><FullCalendar plugins={[dayGridPlugin,timeGridPlugin,listPlugin,interactionPlugin]} initialView="dayGridMonth" locale={ptBr} headerToolbar={{left:'prev,next today',center:'title',right:'dayGridMonth,timeGridWeek,listMonth'}} events={events} height="auto"/></section></main>}
