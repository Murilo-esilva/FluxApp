import {PieChart,Pie,Cell,ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip} from 'recharts'; import {useTasks} from '../shared/useData'; import {STATUSES,PRIORITIES,labelOf} from '../shared/constants';
const colors=['#7db61c','#00ae9d','#45c2b1','#547b83']; export function Reports(){const {data:t=[]}=useTasks();const byStatus=STATUSES.map(([v,l])=>({name:l,value:t.filter(x=>x.status===v).length}));const byPriority=PRIORITIES.map(([v,l])=>({name:l,total:t.filter(x=>x.priority===v).length}));return <main><header className="page-header"><div><p className="eyebrow">Indicadores do MVP</p><h1>Relatórios</h1></div></header><div className="report-grid"><section className="panel chart"><h2>Por status</h2><ResponsiveContainer width="100%" height={280}><PieChart><Pie data={byStatus} dataKey="value" nameKey="name" outerRadius={100} label>{byStatus.map((_,i)=><Cell key={i} fill={colors[i]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></section><section className="panel chart"><h2>Por prioridade</h2><ResponsiveContainer width="100%" height={280}><BarChart data={byPriority}><XAxis dataKey="name"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="total" fill="#00ae9d" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></section></div></main>}
