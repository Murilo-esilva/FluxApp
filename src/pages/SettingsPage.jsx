import {useState} from 'react'; export function SettingsPage(){const [permission,setPermission]=useState(typeof Notification==='undefined'?'indisponível':Notification.permission);async function ask(){if('Notification'in window)setPermission(await Notification.requestPermission())}return <main><header className="page-header"><div><p className="eyebrow">Preferências</p><h1>Configurações</h1></div></header><section className="panel settings"><h2>Notificações do navegador</h2><p>Status: <b>{permission}</b></p><p>O Flux verifica prazos enquanto a aplicação está aberta. Push com o navegador fechado exige a Edge Function descrita no README.</p><button className="primary" onClick={ask}>Permitir notificações</button></section></main>}
